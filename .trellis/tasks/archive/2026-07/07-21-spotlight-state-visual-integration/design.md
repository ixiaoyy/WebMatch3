# Spotlight state/visual integration — design

Snapshot v3 gains a backward-compatible optional pet projection normalized to
`{ guardedPieceId: string | null }`; malformed or stale values default home
without rejecting the game snapshot. Responsive position is derived from the
guarded canonical fish and the same mounted surface moves into PiP.
