# 强化核心玩法与失败反馈：实施计划

## 阶段一：规则闭环

- [x] 更新 `PRODUCT.md`，以自动失败重开替换“满盘永不失败”，保留无失败页面和植物经验不回退。
- [x] 在 engine 类型和转换中以 `lost` 替换 `recovery-needed`，新增纯失败重开转换并移除 `recoverFullTray` 正常路径。
- [x] 覆盖第七条普通入盘、三消优先、猫咪抵扣优先、等级回到 1、托盘/喂食清空、`clearCount`/`nextPieceId` 保留等规则测试。
- [x] controller 增加互斥 `loss` 反馈：立即保存稳定重置状态，保留七格失败预览 1–1.5 秒，并正确处理失焦、销毁和恢复。
- [x] 更新托盘、鱼场和猫咪的最小失败反馈；删除恢复计时器、恢复文案和旧测试。
- [x] 将植物成长改为连续分段曲线，验证第 1 次和第 12 次清除的可见节点以及旧高经验值的单调映射。
- [x] 定向验证：`pnpm --dir apps/web test -- ambient-game ambient-controller game-ui ambient-storage`，然后运行 `pnpm lint:web` 与 `pnpm typecheck:web`。

### 阶段一回滚点

- 规则测试、持久化测试和运行态失败序列同时通过后再进入生成器；否则只回滚本阶段触及的失败/成长路径。

## 阶段二：随机生成

- [ ] 搜索并统一现有布局、重叠和随机工具，避免新建重复随机函数或第二套命中常量。
- [ ] 实现规范化安全区域、有限拒绝采样和确定性回退布局。
- [ ] 随机化平衡层级顺序，并在最终布局后重算重叠元数据。
- [ ] 将旋转改为 `[0, 360)` 完整采样，保持快照解析范围兼容。
- [ ] 保持三元组计数、空间种类分散、首屏可发现匹配和完整清除路径。
- [ ] 增加多种子测试：确定性、安全区、覆盖范围、重叠上限、层级分布、角度象限和完整清除。
- [ ] 定向验证：`pnpm --dir apps/web test -- ambient-game spotlight ambient-storage`，然后运行 `pnpm lint:web` 与 `pnpm typecheck:web`。

### 阶段二回滚点

- 在桌面、窄屏和小窗投影下抽查固定种子布局；任何不可发现、极端堆叠或明显遮挡都阻止进入交互润色。

## 阶段三：交互反馈首轮

- [ ] 在 controller 中定义清晰的反馈状态所有权和取消规则，避免组件各自维护冲突计时器。
- [ ] 增加可中断的首次无文字引导，不阻挡指针、触摸和键盘输入。
- [ ] 依次优化搜索显影、选择入盘、托盘 5/6 格警示、三消、失败、清场、猫咪寻鱼和喂鱼反馈。
- [ ] 复用现有素材完成首轮；只有失败语义仍不清楚时，才生成并接入单张猫咪失败姿态。
- [ ] 为所有新增动效提供 `prefers-reduced-motion` 等价状态。
- [ ] 补充 controller、纯 UI helper 和可访问状态测试，不用只断言 class 存在的空洞测试代替行为验证。
- [ ] 在 1280×720、390×844、键盘路径和 Document Picture-in-Picture 下进行运行态试玩；检查控制台错误。

### 阶段三回滚点

- 每类反馈独立提交和验证；出现输入延迟、焦点丢失或状态不清时，只撤回该类表现，不动已稳定规则。

## 最终验证

- [ ] 运行 `pnpm ci:web`。
- [ ] 检查稳定快照往返、旧版本迁移和存储异常回退。
- [ ] 检查失败后植物经验/种植时间/偏好保留，局内等级、托盘、喂食和守护状态重置。
- [ ] 检查至少一组 64 个固定种子可复现并完整清除。
- [ ] 检查指针、触摸、键盘、窄屏、小窗和减少动态效果路径。
- [ ] 运行最终全范围 Trellis 质量检查，评估是否需要把新的生成/反馈契约写入项目 spec。
